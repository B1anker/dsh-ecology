# Dependencies and build output
node_modules/
dist/
*.tgz
coverage/
.nyc_output/
zig-out*/
.zig-cache/
zig-pkg/
# The desktop companion binary, staged into the pet package at pack time
# (packages/pet build:desktop); never committed, the release workflow rebuilds it.
packages/pet/desktop/
# The same binaries staged into the per-platform optional packages at publish
# time (.github/workflows/publish.yml); never committed either.
packages/pet-desktop-*/bin/

# Local runtime secrets and state
.env
.env.*
!.env.example
*.session
sessions/

# Tooling caches
.rslib/
.rstest/
*.tsbuildinfo
# Declarations belong in dist/. A tsc run whose `paths` reach into a sibling's
# src (the typecheck configs do that on purpose) can drop .d.ts files beside
# the sibling's sources; they must never be committed.
packages/*/src/**/*.d.ts

# Editor and operating-system files
.vscode/
.idea/
*.swp
*~
.DS_Store

# Local install scratch (sandboxed installs)
.bun-cache/
.bun-tmp/
.npm-cache/
