{
  "name": "@seaveyon/dsh-pet-desktop",
  "private": true,
  "version": "0.1.0",
  "description": "Zero-native (Zig + GPU canvas) desktop pet for the DSH Web surface; receives mood state from the dsh-pet plugin over a loopback HTTP bridge",
  "keywords": [
    "dsh",
    "pet",
    "desktop",
    "zero-native",
    "zig"
  ],
  "license": "MIT",
  "author": "B1anker",
  "type": "module",
  "scripts": {
    "build": "node scripts/zig.mjs build",
    "run": "zig build run",
    "test:unit": "bun run test:zig && bun run test:js",
    "test:zig": "node scripts/zig.mjs build test",
    "test:js": "bun test test/",
    "import:codex-pet": "bun scripts/import-codex-pet.mjs"
  }
}
